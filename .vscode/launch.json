{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "gdb",
			"request": "attach",
			
			"name": "Debug in Emulator",
			"executable": "out/kernel.elf",

			"target": ":6001",
			"remote": true,

			"cwd": "${workspaceRoot}"
		}
	]
}
